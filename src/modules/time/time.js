import Control from "control";
import Card from "card";
// const si = require("systeminformation");

export default class Time extends Card{
  constructor(container) {
    super(container, "Time");
  }
}